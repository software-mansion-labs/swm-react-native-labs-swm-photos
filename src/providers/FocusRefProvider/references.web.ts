/**
 * Global reference names
 *
 * References with node handles do not work on web, but we keep this code for compatibility with other platforms
 */
export const references = [];

// Helper function - obtaining reference handle
export function getHandle(ref: React.RefObject<any>) {
  return undefined;
}
